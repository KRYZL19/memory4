"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function storeCredentials(store, sessionLog) {
    return (creds, done) => {
        store.save(creds.actor['@id'], creds, (err) => {
            if (err) {
                sessionLog('error saving credentials to store ' + err);
                done(err);
            }
            else {
                sessionLog('credentials encrypted and saved');
                done();
            }
        });
    };
}
exports.default = storeCredentials;
;
//# sourceMappingURL=/middleware/store-credentials.js.map